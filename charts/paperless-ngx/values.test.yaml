existingEnvSecrets:
- paperless-secrets

envs:
  PAPERLESS_URL: https://paperless.example.com
  PAPERLESS_ALLOWED_HOSTS: "*"

redis:
  existingSecretPasswordKey: VALKEY_PASSWORD

oidc:
  enabled: true
  serverUrl: https://idp.example.com/.well-known/openid-configuration

admin:
  existingSecret: paperless-secrets

persistence:
  existingClaim: paperless-data

app:
  extraVolumes:
  - name: branding
    configMap:
      name: paperless-branding
  extraVolumeMounts:
  - name: branding
    mountPath: /branding
    readOnly: true
  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
    - host: paperless.example.com
      paths:
      - /
    tls:
    - hosts:
      - paperless.example.com
      secretName: paperless-tls

networkPolicy:
  enabled: true
