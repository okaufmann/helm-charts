apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "node-app.fullname" . }}-config
  labels:
    {{- include "node-app.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.envs }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
